# GitHub Pages

Publish this `docs/` directory with GitHub Pages.

The page is dependency-free and uses only HTML, CSS, and JavaScript.

It is designed to communicate the architecture in three minutes without exposing private implementation details.
